#
# ~/.bash_profile
# Refer to .bashrc for everything
# If you are on a system that doesn't read .bash_profile by default (can test
# by putting an echo command in it), don't bother doing anything with this.
#

. ~/.bashrc
